{
  "scripts": {
    "start": "node index.js",
    "build": "npm install && rimraf public && mkdir public && copy * public"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "puppeteer": "^21.5.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
